runtime: typescript
config:
  supabaseKey:
    type: string
    description: Supabase service role key (NOT anon key) - required for full database access including insert/update/delete operations. Find this in Supabase Dashboard → Settings → API → service_role key
    required: true